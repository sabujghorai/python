roll_number = {281,291,286,294}
print(roll_number)
print(type(roll_number)) 
print(len(roll_number)) # prints the length of the sets

# we can make a empty set also 
roll = set() # syntax of empty set
